<div class='container'>
  <h1>Lists</h1>

  <% @lists.each do |list| %>
    <%= link_to list.name, list_path(list) %>
  <% end %>


  <div>
    <%= link_to "New List", new_list_path, class: "btn btn-primary" %>
  </div>
</div>
